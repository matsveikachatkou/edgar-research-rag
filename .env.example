# Required
OPENAI_API_KEY=sk-...

# Optional — only needed if adding PDF support via Mistral OCR
# MISTRAL_API_KEY=...

# Optional — comma-separated tickers for the automated pipeline scanner
# WATCHED_TICKERS=AAPL,TSLA,MSFT,NVDA
